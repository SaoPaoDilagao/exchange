server:
  port: '8103'

spring:
  profiles:
    active: @activatedProperties@
  application:
    name: exchange

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: 'true'
    restart:
      enabled: 'true'
